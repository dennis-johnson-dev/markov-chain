// @flow

import Markov from '../markov';

test('initializes text', async () => {
  const foo: string = 'hai';
});

test('generates new random text', async () => {
  const foo: string = 'hai';
  expect('3').toBe('3');
});
